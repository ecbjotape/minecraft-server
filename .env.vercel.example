# Variáveis de ambiente necessárias para o Vercel
# Configure estas variáveis no painel da Vercel

# ID da instância EC2
INSTANCE_ID=

# Elastic IP da instância
EIP=

# Usuário SSH
USER=

# Conteúdo do arquivo PEM (substitua \n por quebras de linha literais)
# PEM_CONTENT="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"

# Credenciais AWS (para executar comandos AWS CLI)
# AWS_ACCESS_KEY_ID=seu_access_key
# AWS_SECRET_ACCESS_KEY=seu_secret_key
# AWS_REGION=us-east-1

# Autenticação (opcional)
# Habilita/desabilita autenticação no dashboard
AUTH_ENABLED=false

# Usuários autorizados (formato: username:salt:hash,username2:salt:hash2)
# Use o script: node scripts/generate-password-hash.js <username> <password>
# AUTH_USERS=admin:abc123...def456:789ghijk...xyz,user2:salt2:hash2
